import * as ActionTypes from "actions/actionTypes";
import { getActionSuccess, getActionFailed } from "actions";
const {{name}}InitialState = {
  loading: false,
  error: null,
  data: null
};

function {{name}}(state = {{name}}InitialState, action: any) {
  switch (action.type) {
    default:
      return state;
  }
}

export default {{name}};
